import { createTheme } from '@mui/material/styles';

const theme = createTheme({
     palette: {
           mode: 'light',
    primary: {
      main: '#1b5e20',
    },
    secondary: {
      main: '#ff3d00',
    },
    background: {
      default: '#d7dbde',
        },
  },
});

export default theme;